<nav class="pagination" role="navigation">
    {{#if prev}}
        <a class="newer-posts" href="{{page_url prev}}">&larr; Nyere artikler</a>
    {{/if}}
    <span class="page-number">Side {{page}} av {{pages}}</span>
    {{#if next}}
        <a class="older-posts" href="{{page_url next}}">Eldre artikler &rarr;</a>
    {{/if}}
</nav>